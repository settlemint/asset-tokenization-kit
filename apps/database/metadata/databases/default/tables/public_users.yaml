table:
  name: users
  schema: public
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: userId
        table:
          name: accounts
          schema: public
  - name: private_keys
    using:
      foreign_key_constraint_on:
        column: userId
        table:
          name: private_keys
          schema: public
  - name: sessions
    using:
      foreign_key_constraint_on:
        column: userId
        table:
          name: sessions
          schema: public
  - name: user_roles
    using:
      foreign_key_constraint_on:
        column: userId
        table:
          name: user_roles
          schema: public
