{
	"name": "contracts",
	"version": "1.0.0",
	"private": false,
	"license": "FSL-1.1-MIT",
	"author": {
		"name": "SettleMint",
		"email": "support@settlemint.com",
		"url": "https://settlemint.com"
	},
	"scripts": {
		"dependencies": "forge soldeer install && find dependencies -name .git -type d -exec rm -rf {} +",
		"lint:biome": "biome lint --write --unsafe .",
		"lint:solhint": "solhint --config .solhint.json ./contracts/**/*.sol",
		"format:biome": "biome format --write .",
		"format:forge": "forge fmt",
		"compile:forge": "forge build --sizes",
		"compile:hardhat": "hardhat compile",
		"test": "forge test -vvv",
		"test:deploy": "hardhat ignition deploy --network localhost ignition/modules/main.ts --reset",
		"genesis": "bash -ex genesis-output.sh",
		"deploy:remote": "settlemint scs hardhat deploy remote",
		"prod:deploy": "turbo deploy:remote"
	},
	"dependencies": {
		"@nomicfoundation/hardhat-foundry": "1.1.3",
		"@nomicfoundation/hardhat-toolbox-viem": "3.0.0",
		"@nomiclabs/hardhat-solhint": "4.0.1",
		"hardhat": "2.22.18",
		"solhint": "5.0.5"
	},
	"devDependencies": {},
	"peerDependencies": {},
	"overrides": {
		"elliptic": "6.6.1",
		"ws": "8.18.0"
	}
}
