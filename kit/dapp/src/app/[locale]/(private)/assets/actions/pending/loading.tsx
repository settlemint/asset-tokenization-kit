import { AssetEventsSkeleton } from "@/components/blocks/asset-events-table/asset-events-skeleton";

export default function Loading() {
  // Display table skeleton as the primary loading state
  return <AssetEventsSkeleton />;
}
