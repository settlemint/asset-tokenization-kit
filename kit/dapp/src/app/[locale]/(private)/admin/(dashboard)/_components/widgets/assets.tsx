'use client';

import { Widget } from './widget';

export function AssetsWidget() {
  return (
    <Widget
      label="Assets supply"
      value="1"
      subtext="to be moved to the graph"
    />
  );
}
