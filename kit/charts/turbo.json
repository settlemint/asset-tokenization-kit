{
  "extends": ["//"],
  "tasks": {
    "dependencies": {
      "outputs": ["**/Chart.lock", "**/charts/**"],
      "inputs": ["**/Chart.yaml"]
    },
    "compile": {
      "outputs": ["dist/**"]
    }
  }
}
