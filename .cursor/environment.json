{
  "install": "bun install",
  "start": "sudo systemctl start docker && bun run artifacts && bun run dev:up",
  "terminals": [
    {
      "name": "dev",
      "command": "bun run dev"
    }
  ],
  "build": {
    "context": ".",
    "dockerfile": "Dockerfile"
  }
}
