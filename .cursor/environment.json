{
  "install": "bun install",
  "start": "sudo systemctl start docker && docker-compose up -d && bun run artifacts && bun run codegen",
  "terminals": [],
  "build": {
    "context": ".",
    "dockerfile": "Dockerfile"
  }
}
