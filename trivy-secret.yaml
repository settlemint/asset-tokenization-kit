rules:
  - id: settlemint
    category: token
    title: SettleMint Access Token Detection
    severity: HIGH
    keywords:
      - sm_pat
      - sm_aat
      - sm_sat
    regex: (?i)(?P<key>(sm_(pat|aat|sat)))(_)(?P<secret>[0-9a-zA-Z]+)
    secret-group-name: secret