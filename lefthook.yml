pre-commit:
  parallel: true
  jobs:
    - name: format
      run: bun run format
      stage_fixed: true
    - name: docs-and-stage
      run: bun run docs && git add -u && git add . --ignore-errors
