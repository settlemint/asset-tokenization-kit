# Local Development Environment Configuration
# Copy this file to .env.local and update with your actual values
#
# Quick Start:
#   cp .env.local.example .env.local
#   # Then edit .env.local with your real credentials
#
# This file can be safely committed to git (contains only dummy values)
# .env.local is in .gitignore and should contain your real secrets

# ============================================================================
# GitHub Container Registry Authentication
# ============================================================================
# Required to pull private Docker images from ghcr.io/settlemint/*
# Get your token from: https://github.com/settings/tokens
# Required scope: read:packages
#
# IMPORTANT: Replace with your actual GitHub credentials!
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GITHUB_USERNAME=your_github_username

# ============================================================================
# Hasura Configuration
# ============================================================================
# Admin secret for Hasura GraphQL console access
SETTLEMINT_HASURA_ADMIN_SECRET=hasura

# Database connection URL for Hasura
SETTLEMINT_HASURA_DATABASE_URL=postgresql://hasura:hasura@localhost:5432/hasura

# ============================================================================
# Blockchain Configuration
# ============================================================================
# JSON-RPC endpoint for blockchain interactions
# Points to TxSigner which wraps Anvil for transaction management
SETTLEMINT_BLOCKCHAIN_NODE_JSON_RPC_ENDPOINT=http://localhost:8547

# ============================================================================
# MinIO Configuration
# ============================================================================
# Secret key for MinIO service account (used by dApp for file storage)
SETTLEMINT_MINIO_SECRET_KEY=atk-service-secret

# ============================================================================
# Database Migrations
# ============================================================================
# Set to true to prevent automatic migrations on dApp startup
# dev.sh handles migrations manually for better control
DISABLE_MIGRATIONS_ON_STARTUP=true

