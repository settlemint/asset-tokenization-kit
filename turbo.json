{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "dangerouslyDisablePackageManagerCheck": true,
  "envMode": "loose",
  "tasks": {
    "connect": {
      "outputs": ["settlemint.config.mjs", ".env.local"]
    },
    "codegen": {
      "dependsOn": ["^codegen", "contracts#build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["generated/**", ".settlemint/**"]
    },
    "build": {
      "dependsOn": ["^build", "codegen", "^codegen"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "out/**", "build/**"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "test": {
      "dependsOn": ["^test"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "translate": {
      "inputs": ["messages/**"]
    }
  }
}
